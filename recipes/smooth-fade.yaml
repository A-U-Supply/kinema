name: Smooth Fade
description: >
  Gentle cross-dissolves between every image. The default — pure xfade
  with no embellishment. Use as a baseline to verify the pipeline is
  working before reaching for anything wilder.

transitions:
  - type: xfade
    weight: 1.0
    params:
      mode: fade
      duration: 0.5
