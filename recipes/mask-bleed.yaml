name: Mask Bleed
description: >
  Each cut a different shape pulled from the mask pool — wipes, slices,
  circles, opens. The video reads as a sequence of unmaskings rather than
  a sequence of dissolves.

transitions:
  - type: mask
    weight: 1.0
    params:
      mode: random
      duration: 0.6
